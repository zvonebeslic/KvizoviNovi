name: Fix Moreplovci from 800
on:
  push:
    paths: ['MOREPLOVCI_800_TRIGGER.txt']
  workflow_dispatch:
permissions:
  contents: write
jobs:
  fix:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: {python-version: '3.12'}
      - run: python scripts/fix_moreplovci_from_800.py
      - run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add Moreplovci.json
          git diff --cached --quiet || (git commit -m 'Popravi Moreplovce od retka 800' && git push)
