name: Fix Zemljopis 421-520
on:
  push:
    paths:
      - 'ZEMLJOPIS_421_520_TRIGGER.txt'
  workflow_dispatch:
jobs:
  fix:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - run: python scripts/fix_zemljopis_421_520.py
      - run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add Zemljopis.json
          git diff --cached --quiet || git commit -m "Ručno popravi Zemljopis 421-520"
          git push
