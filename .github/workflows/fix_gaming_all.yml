name: Fix Gaming all ABC
on:
  push:
    paths:
      - 'scripts/fix_gaming_all.py'
  workflow_dispatch:
permissions:
  contents: write
jobs:
  fix:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Fix Gaming ABC distractors
        run: python scripts/fix_gaming_all.py
      - name: Commit Gaming
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add Gaming.json
          if git diff --cached --quiet; then exit 0; fi
          git commit -m 'Fix all Gaming ABC distractors'
          git push
