name: Fix Zemljopis 321-420
on:
  push:
    paths:
      - 'ZEMLJOPIS_321_420_TRIGGER.txt'
  workflow_dispatch:
jobs:
  fix:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - run: python scripts/fix_zemljopis_321_420.py
      - run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add Zemljopis.json
          git diff --cached --quiet || git commit -m "Ručno popravi Zemljopis 321-420"
          git push
